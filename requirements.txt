mcp[cli]>=1.0.0
monarchmoneycommunity
keyring>=24.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Security floors — transitive deps pinned to patched versions on the community
# (monarchmoneycommunity) line. jaraco-context is intentionally omitted: unlike
# the monarchmoney line it is not pulled here. Keep in sync with the venv.
idna>=3.15                # GHSA-65pc-fj4g-8rjx  (DoS via httpx)
aiohttp>=3.13.4           # GHSA-63hf-3vf5-4wqf  (CRITICAL) — venv had 3.13.3
cryptography>=46.0.7      # GHSA-r6ph-v2qm-q3c2 / PYSEC-2026-36 — venv had 46.0.4
starlette>=0.49.1         # GHSA-7f5h-v6xp-fcq8 / GHSA-f96h-pmfr-66vw
python-multipart>=0.0.22  # GHSA-wp53-j4wj-2cfg / GHSA-59g5-xgcq-4qw3
